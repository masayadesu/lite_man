<h1>会員の詳細</h1>
<table class="table text-left">
  <div class="card-header">
    <h4>会員詳細</h4>
  </div>
  <div class="card-body ">
    <tr>
      <th>ID</th>
      <td><%= @user.id %></td>
    </tr>
    <tr>
      <th>ユーザ名</th>
      <td><%= @user.name %></td>
    </tr>
    <tr>
      <th>管理者</th>
      <td><%#= @user.administrator? ? "○": "-" %></td>
    </tr>
