<%= render 'shared/error_messages' %>

<div class="form_box  col-sm-6">
  <h2>アカウント情報の編集</h2>
  <%= form_with model: @user, url: :account do |form| %>
    <%= render 'users/form', form: form %>
  <%= form.submit '更新', class: 'btn btn-primary' %>
  <% end %>
</div>
